# Provider API keys (leave blank locally; set via your shell or an untracked .env)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Default LLM-judge model (placeholder; override per environment)
TOKENSURF_JUDGE_MODEL=gpt-4o-mini
